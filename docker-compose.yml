version: "3.9"

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bewize-dashboard-monitor-api
    environment:
      SPRING_PROFILES_ACTIVE: dev
      SPRING_DATASOURCE_URL: jdbc:postgresql://ep-autumn-band-am3ylbfg-pooler.c-5.us-east-1.aws.neon.tech/neondb?sslmode=require
      SPRING_DATASOURCE_USERNAME: neondb_owner
      SPRING_DATASOURCE_PASSWORD: TON_NOUVEAU_MOT_DE_PASSE
      SERVER_PORT: 8080
    ports:
      - "8080:8080"
    restart: unless-stopped
