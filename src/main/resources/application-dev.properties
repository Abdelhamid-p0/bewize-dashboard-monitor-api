spring:
  datasource:
    url: jdbc:postgresql://ep-autumn-band-am3ylbfg-pooler.c-5.us-east-1.aws.neon.tech/neondb?sslmode=require
    username: neondb_owner
    password: TON_NOUVEAU_MOT_DE_PASSE
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

app:
  cors:
    allowed-origins: "http://localhost:5173,https://abdelhamid0p.github.io"

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always